{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/shuttlehub run build",
  "outputDirectory": "artifacts/shuttlehub/dist/public",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
